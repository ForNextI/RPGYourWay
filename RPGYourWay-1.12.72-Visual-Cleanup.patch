diff --git a/app/globals.css b/app/globals.css
index 054d633..3105314 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -392,9 +392,20 @@ h1, h2, h3 { font-family: Georgia, Cambria, "Times New Roman", serif; color: var
   align-items: center;
   justify-content: space-between;
   gap: 1.25rem;
+  border: 2px solid var(--rpgyw-brass-edge);
+  border-radius: 18px;
+  background: linear-gradient(180deg, color-mix(in srgb, var(--landing-pale-olive-light) 88%, white), var(--landing-pale-olive));
   padding: 1rem 1.1rem 1rem 1.25rem;
+  box-shadow:
+    inset 0 0 0 1px color-mix(in srgb, var(--rpgyw-brass-highlight) 72%, white),
+    inset 0 1px 0 rgb(255 255 255 / .72),
+    0 3px 0 var(--rpgyw-brass-shadow),
+    0 8px 14px rgb(43 47 38 / .11);
+}
+.landing-accordion-summary:hover,
+.landing-accordion-summary:focus-visible {
+  background: linear-gradient(180deg, color-mix(in srgb, var(--landing-pale-olive-light) 94%, white), var(--landing-pale-olive));
 }
-.landing-accordion-summary:hover { background: rgb(117 134 90 / .08); }
 .landing-accordion-prompt {
   font-family: Georgia, Cambria, "Times New Roman", serif;
   color: var(--forest-deep);
@@ -6871,10 +6882,17 @@ html {
   align-items: flex-start;
   justify-content: space-between;
   gap: .65rem;
-  border-radius: 13px;
-  background: var(--forest-deep);
+  border: 2px solid var(--rpgyw-brass-edge);
+  border-radius: 14px;
+  background: linear-gradient(180deg, color-mix(in srgb, var(--forest-deep) 94%, white), var(--forest-deep));
   padding: .65rem .75rem;
   color: var(--cream-bright);
+  box-shadow:
+    inset 0 0 0 1px color-mix(in srgb, var(--rpgyw-brass-highlight) 68%, white),
+    inset 0 1px 0 rgb(255 255 255 / .15),
+    inset 0 -2px 0 rgb(0 0 0 / .12),
+    0 3px 0 var(--rpgyw-brass-shadow),
+    0 7px 12px rgb(29 38 29 / .12);
 }
 .aigm-table-chat-eyebrow {
   margin: 0 0 .08rem;
@@ -7023,11 +7041,16 @@ html {
   min-height: 7rem;
   flex: 1 1 auto;
   overflow-y: auto;
-  margin-top: .5rem;
-  border: 1px solid var(--rpgyw-brass-shadow);
-  border-radius: 12px;
+  margin: .65rem .15rem .15rem;
+  border: 6px solid var(--landing-olive-19);
+  border-radius: 14px;
   background: var(--background);
-  padding: .55rem;
+  padding: .7rem;
+  box-shadow:
+    0 0 0 2px var(--rpgyw-brass-edge),
+    inset 0 0 0 1px color-mix(in srgb, var(--rpgyw-brass-highlight) 70%, white),
+    inset 0 5px 9px rgb(82 60 28 / .11),
+    0 3px 0 var(--rpgyw-brass-shadow);
   scrollbar-gutter: stable;
 }
 .aigm-table-chat-empty {
diff --git a/lib/version.ts b/lib/version.ts
index 82d4a8c..2334e0a 100644
--- a/lib/version.ts
+++ b/lib/version.ts
@@ -1 +1 @@
-export const APP_VERSION = '1.12.1'
+export const APP_VERSION = '1.12.72'
diff --git a/package.json b/package.json
index 354a31f..032cdee 100644
--- a/package.json
+++ b/package.json
@@ -1,6 +1,6 @@
 {
   "name": "rpg-your-way",
-  "version": "1.12.1",
+  "version": "1.12.72",
   "private": true,
   "packageManager": "pnpm@10.14.0",
   "scripts": {
@@ -27,5 +27,5 @@
     "postcss": "^8.5",
     "tailwindcss": "^4.3.3"
   },
-  "rpgywVersion": "1.12.1"
+  "rpgywVersion": "1.12.72"
 }
diff --git a/scripts/validate-release.mts b/scripts/validate-release.mts
index 647735f..62dd8ca 100644
--- a/scripts/validate-release.mts
+++ b/scripts/validate-release.mts
@@ -20,9 +20,9 @@ const pkg = JSON.parse(read('package.json')) as {
   dependencies?: Record<string, string>
 }
 assert.equal(pkg.name, 'rpg-your-way')
-assert.equal(pkg.version, '1.12.1')
-assert.equal(pkg.rpgywVersion, '1.12.1')
-has(read('lib/version.ts'), "APP_VERSION = '1.12.1'", 'visible app version')
+assert.equal(pkg.version, '1.12.72')
+assert.equal(pkg.rpgywVersion, '1.12.72')
+has(read('lib/version.ts'), "APP_VERSION = '1.12.72'", 'visible app version')
 
 for (const file of [
   'app/page.tsx',
@@ -218,4 +218,4 @@ has(accessibility, 'Accessibility')
 has(css, 'outline: 3px solid var(--lime);')
 assert.match(css, /background:\s*linear-gradient\(\s*180deg,\s*color-mix\(in srgb, var\(--cream-bright\) 97%, white\),\s*var\(--cream\)\s*\) !important;/)
 
-console.log('RPG Your Way 1.12.1 deliberate cleanup pass 1 checks passed.')
+console.log('RPG Your Way 1.12.72 cleanup-series checks passed.')
