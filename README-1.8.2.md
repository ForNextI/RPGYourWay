# RPG Your Way 1.8.2

Start-page QA pass. Adds the visible step 1 marker to Choose the game rules, renames step 2 to Default party and step 3 to Customize or Default Settings, compacts and standardizes the campaign-question layouts, replaces the Sharn/Malcolm placeholders with neutral player-facing naming prompts, removes the Game Master voice chooser because the default is male/Fable, and removes the internal character-standardization UI-review note from the player interface.
